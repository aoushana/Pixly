<h3>Home</h3>
<% @images.each do |image| %>
    <div class = "image">
      <%= link_to image_tag(image.file.url(:medium)), image %>
    </div>
<% end %>

<!--images.forEach(function (image) {-->
<!--});-->
